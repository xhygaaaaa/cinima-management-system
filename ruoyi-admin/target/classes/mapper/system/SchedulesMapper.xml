<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SchedulesMapper">
    
    <resultMap type="Schedules" id="SchedulesResult">
        <result property="scheduleId"    column="schedule_id"    />
        <result property="cinemaId"    column="cinema_id"    />
        <result property="hallId"    column="hall_id"    />
        <result property="movieId"    column="movie_id"    />
        <result property="showTime"    column="show_time"    />
        <result property="price"    column="price"    />
        <result property="seatsAvailable"    column="seats_available"    />
    </resultMap>

    <sql id="selectSchedulesVo">
        select schedule_id, cinema_id, hall_id, movie_id, show_time, price, seats_available from schedules
    </sql>

    <select id="selectSchedulesList" parameterType="Schedules" resultMap="SchedulesResult">
        <include refid="selectSchedulesVo"/>
        <where>  
            <if test="cinemaId != null "> and cinema_id = #{cinemaId}</if>
            <if test="hallId != null "> and hall_id = #{hallId}</if>
            <if test="movieId != null "> and movie_id = #{movieId}</if>
            <if test="showTime != null "> and show_time = #{showTime}</if>
            <if test="price != null "> and price = #{price}</if>
            <if test="seatsAvailable != null "> and seats_available = #{seatsAvailable}</if>
        </where>
    </select>
    
    <select id="selectSchedulesByScheduleId" parameterType="Long" resultMap="SchedulesResult">
        <include refid="selectSchedulesVo"/>
        where schedule_id = #{scheduleId}
    </select>

    <insert id="insertSchedules" parameterType="Schedules" useGeneratedKeys="true" keyProperty="scheduleId">
        insert into schedules
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="cinemaId != null">cinema_id,</if>
            <if test="hallId != null">hall_id,</if>
            <if test="movieId != null">movie_id,</if>
            <if test="showTime != null">show_time,</if>
            <if test="price != null">price,</if>
            <if test="seatsAvailable != null">seats_available,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="cinemaId != null">#{cinemaId},</if>
            <if test="hallId != null">#{hallId},</if>
            <if test="movieId != null">#{movieId},</if>
            <if test="showTime != null">#{showTime},</if>
            <if test="price != null">#{price},</if>
            <if test="seatsAvailable != null">#{seatsAvailable},</if>
         </trim>
    </insert>

    <update id="updateSchedules" parameterType="Schedules">
        update schedules
        <trim prefix="SET" suffixOverrides=",">
            <if test="cinemaId != null">cinema_id = #{cinemaId},</if>
            <if test="hallId != null">hall_id = #{hallId},</if>
            <if test="movieId != null">movie_id = #{movieId},</if>
            <if test="showTime != null">show_time = #{showTime},</if>
            <if test="price != null">price = #{price},</if>
            <if test="seatsAvailable != null">seats_available = #{seatsAvailable},</if>
        </trim>
        where schedule_id = #{scheduleId}
    </update>

    <delete id="deleteSchedulesByScheduleId" parameterType="Long">
        delete from schedules where schedule_id = #{scheduleId}
    </delete>

    <delete id="deleteSchedulesByScheduleIds" parameterType="String">
        delete from schedules where schedule_id in 
        <foreach item="scheduleId" collection="array" open="(" separator="," close=")">
            #{scheduleId}
        </foreach>
    </delete>
</mapper>